# Set as a build-time environment variable on the Cloudflare Worker (or put it in
# .env.production for a local production build). Leave unset in development: the Vite
# dev proxy handles /api instead.
VITE_API_BASE=https://tickets-6cko.onrender.com
