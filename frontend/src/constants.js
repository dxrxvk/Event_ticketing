// Mirrors MAX_TICKETS_PER_BOOKING in tickets/models.py. The server is authoritative and
// rejects anything larger; this only stops the form offering what will be refused.
export const MAX_GUESTS = 8
